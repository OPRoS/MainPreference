package org.opros.mainpreference.preferences;

import java.net.MalformedURLException;
import java.net.URL;

import org.eclipse.jface.action.Action;
import org.eclipse.jface.preference.IPreferenceStore;
import org.eclipse.jface.resource.ImageDescriptor;
import org.opros.mainpreference.Activator;


public class PreferenceImages extends Action {
//private static ImageDescriptor image;
/*
static {

  URL url = null;
  try {
  url = new URL(MainPreferencePages.getInstance().getDescriptor().getInstallURL(),
                "images/my_action.gif");
  } catch (MalformedURLException e) {
  }
  image = ImageDescriptor.createFromURL(url);
}
*/
}